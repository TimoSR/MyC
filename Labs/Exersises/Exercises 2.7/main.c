/* Exercise 2.7
 *
 * Name: Timothy Stoltzer Rasmussen
 *
 * Date of creation: 03/03/18
 *
 * Last edit: 03/03/18
 *
 */

#include <stdio.h>

int value, x, y, a, b, c, sum, number, largest, z;

float hi;

int main() {

    scanf("%d \n", value);

    printf("The sum of %d and %d is %d \n", x, y, x + y);

    sum = a + b + c;

    if (number > largest) {

        largest == number;

    }

    // Program to determine the largest of three integers

    scanf("%f", hi);

    printf("Remainder of %d divided by %d is %f \n", x, y, x / y);

    if (x >= y);

    printf("%d is greater than or equal to %d\n, x, y");

    printf("The product is %d \n", x * y);

    scanf("%d, %d, %d", x, y, z);

    return 0;

}